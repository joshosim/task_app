import 'package:flutter/material.dart';

class TrialScreen extends StatelessWidget {
  const TrialScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Triral screen')),
    );
  }
}
